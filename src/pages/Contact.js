import React, { useContext } from "react";
import { CounterButtons } from "../Component/CounterButtons";

const Contact = () => {
  return (
    <div>
      <h1>on the contact page</h1>
      <CounterButtons></CounterButtons>
    </div>
  );
};

export default Contact;
